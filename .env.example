# Copy to .env for local dev:  cp .env.example .env
# Vite inlines VITE_* at build time — set the same variables in Vercel for production.

VITE_SUPABASE_URL=https://trlegeualmmmkcwomdtm.supabase.co

# Use either name (same Supabase anon/public key from Dashboard → Settings → API):
VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_public_key
# VITE_SUPABASE_ANON_KEY=your_supabase_anon_public_key
